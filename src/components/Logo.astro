---
import { Image } from 'astro:assets';
import logoSrc from '@/assets/logo-navbar-128.png';

/**
 * Logo Component
 *
 * Vashon Software brand logo with wordmark.
 * Used consistently across Header, Footer, and Auth layouts.
 *
 * @example
 * ```astro
 * <Logo />
 * <Logo showText={false} />
 * <Logo size="lg" />
 * ```
 */
interface Props {
  /** Show the "AstroDeck" text next to the icon */
  showText?: boolean;
  /** Size variant */
  size?: 'sm' | 'md' | 'lg' | 'xl' | '2xl';
  /** Additional CSS classes */
  class?: string;
}

const { showText = true, size = 'md', class: className = '' } = Astro.props;

const sizeClasses = {
  sm: 'h-5 w-5',
  md: 'h-6 w-6',
  lg: 'h-8 w-8',
  xl: 'h-10 w-10',
  '2xl': 'h-14 w-14',
};

const imageSizes = {
  sm: 20,
  md: 24,
  lg: 32,
  xl: 40,
  '2xl': 56,
};

const textSizeClasses = {
  sm: 'text-sm',
  md: 'text-base',
  lg: 'text-lg',
  xl: 'text-xl',
  '2xl': 'text-2xl',
};
---

<a href="/" class:list={['flex items-center space-x-2', className]}>
  <Image
    src={logoSrc}
    alt="Vashon Software logo"
    width={imageSizes[size]}
    height={imageSizes[size]}
    class={sizeClasses[size]}
    loading="eager"
    fetchpriority="high"
    format="webp"
  />
  {showText && <span class:list={['font-bold', textSizeClasses[size]]}>Vashon Software</span>}
</a>
